import React from "react";
import PropTypes from "prop-types";

/**
 * WeeklyActivityTitle component that displays the title for the weekly activity section.
 * @component
 * @returns {JSX.Element} The rendered WeeklyActivityTitle component
 */
const WeeklyActivityTitle = () => {
    return <h2 className="weekly_activity-title">Activité quotidienne</h2>;
};

// There is no PropTypes here.

export default WeeklyActivityTitle;
